{
  "name": "repass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "10.16.3"
  },
  "scripts": {
    "start": "npm run buildscript",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "buildscript": "cd client && npm run build && cd ../server && npm run prod",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run start",
    "install-dependencies": "rm -rf node_modules && npm install && npm run server-install && npm run client-install",
    "client-install": "cd client && rm -rf node_modules && npm install",
    "server-install": "cd server && rm -rf node_modules && npm install",
    "test-server": "cd server && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.2",
    "dotenv": "^8.1.0"
  }
}
